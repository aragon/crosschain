[profile.default]
src = "src"
out = "out"
libs = ["lib"]

# `CrossChainController` exceeds the 24,576-byte EIP-170 limit unoptimized.
# `runs` trades deployed size against per-call gas: lower favours smaller code.
optimizer = true
optimizer_runs = 200

# The test suite reads its chain id / CCIP selector table from JSON. Read-only,
# and scoped to that one directory.
fs_permissions = [
    { access = "read", path = "./test/fixtures" },
]

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options

[fmt]
single_line_statement_blocks = "preserve"
line_length = 120
int_types = "long"
bracket_spacing = true
multiline_func_header = "all"
quote_style = "double"
tab_width = 4
wrap_comments = true
ignore = ["node_modules", "out", "cache", "lib", "coverage"]